name: Auto Update Feux de Forêt — Temps Réel

on:
  workflow_dispatch:           # déclenché par cron-job.org toutes les 5 min

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update-data:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt


      - name: Run Fetch Feux Script
        run: python fetch_feux.py --ci
        env:
          CI: "true"
          SMTP_USER: "gregory.langlet@sfr.fr"
          SMTP_PASS: "9a?h,h@vxe7K42'"
          SMTP_SERVER: "smtp.sfr.fr"
          SMTP_PORT: "465"

      - name: Commit and Push Updated Site
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Feux Bot"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/mto-user84925/feux-foret-vercel.git"
          git add index.html Rapport_Feux_de_Foret_Temps_Reel.pdf data/ || true
          git diff --staged --quiet && echo "Rien à committer" && exit 0
          git commit -m "🔥 Auto-refresh feux $(date -u +'%H:%M UTC') [skip ci]"
          for i in {1..5}; do
            git pull --rebase origin main && git push && break || sleep 3
          done

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

